<article>
	<slot name="title" />

	<slot name="image" />

	<slot />
</article>

<style>
	article {
		display: grid;
		grid-template-columns: 100%;
		grid-template-rows: auto;
		place-content: center;
		place-items: center;
		gap: 1em;
		
		font-size: var(--font-size-base);
		padding: 1em 1em 0px;
		margin: var(--margin-page-h);
	}
	
	article > :global(*) {
		margin: 0;
	}
	
	article > :global(img) {
		width: 100%;
		background: white;
	}
	
	@media screen and (min-width: 768px) {
		article {
			grid-template-columns: repeat(2, min(50%, 25rem));
			grid-template-rows: repeat(2, auto);
			gap: 3em;
			
		}
		
		article > :global(img) {
			grid-column: 1;
			grid-row: 1 / 3;
		}
		
		article:nth-child(2n) > :global(img) {
			grid-column: 2;
		}
	}
</style>
